/**
 * Page-local MDX components for workers/inference.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { InferenceWorkerExamples } from "./InferenceWorkerExamples";
import { InferenceWorkerVariantSchemaEmbed } from "./InferenceWorkerVariantSchemaEmbed";

export const pageMdxComponents = {
  InferenceWorkerExamples,
  InferenceWorkerVariantSchemaEmbed,
};
