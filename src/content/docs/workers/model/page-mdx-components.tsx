/**
 * Page-local MDX components for workers/model.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { ModelWorkerExamples } from "./ModelWorkerExamples";
import { ModelWorkerVariantSchemaEmbed } from "./ModelWorkerVariantSchemaEmbed";

export const pageMdxComponents = {
  ModelWorkerExamples,
  ModelWorkerVariantSchemaEmbed,
};
