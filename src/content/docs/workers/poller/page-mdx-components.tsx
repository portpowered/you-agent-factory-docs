/**
 * Page-local MDX components for workers/poller.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { PollerWorkerExamples } from "./PollerWorkerExamples";
import { PollerWorkerVariantSchemaEmbed } from "./PollerWorkerVariantSchemaEmbed";

export const pageMdxComponents = {
  PollerWorkerExamples,
  PollerWorkerVariantSchemaEmbed,
};
