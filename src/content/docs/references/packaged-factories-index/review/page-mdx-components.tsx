/**
 * Review-owned page MDX component map for packaged-factories-index/review.
 *
 * Literal-imported by the route-family loader so this route resolves the
 * review-only replay mount instead of the shared replay-page placeholder.
 * Keep recording imports inside ReviewFactoryReplay — never pull sibling
 * packaged-factory recordings into this map module.
 */
import { ReviewFactoryReplay } from "./ReviewFactoryReplay";

export const pageMdxComponents = {
  ReviewFactoryReplay,
};
