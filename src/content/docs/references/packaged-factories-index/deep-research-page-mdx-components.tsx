/**
 * Non-replay page MDX component map for packaged-factories-index/deep-research.
 *
 * deep-research must never resolve the shared standard-replay child map.
 * Thin placeholder until Batch 4 authors the deep-research child page; keep
 * this free of factory-replay, recordings, visualizers, and playback state.
 */
export const packagedFactoriesIndexChildComponentMapKind =
  "non-replay" as const;

/**
 * Plain non-replay MDX map for the deep-research nested slug.
 */
export const pageMdxComponents = {};
