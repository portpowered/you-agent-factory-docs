/**
 * Fusion-owned page MDX component map for packaged-factories-index/fusion.
 *
 * Literal-imported by the route-family loader so this route resolves the
 * fusion-only replay mount instead of the shared replay-page placeholder.
 * Keep recording imports inside FusionFactoryReplay — never pull sibling
 * packaged-factory recordings into this map module.
 */
import { FusionFactoryReplay } from "./FusionFactoryReplay";

export const pageMdxComponents = {
  FusionFactoryReplay,
};
