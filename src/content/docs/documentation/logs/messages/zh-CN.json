{
  "title": "Logs",
  "description": "Find you-agent-factory runtime logs under the selected log root, manage rolling retention, and use CLI --verbose / --debug diagnostics without confusing logs with metrics or record/replay.",
  "openingSummary": "Runtime logs are the you-agent-factory on-disk channel for structured JSON rolling files under a selected log root, separate from stderr CLI diagnostics and from runtime metrics.",
  "sections": {
    "howToUse": {
      "title": "How To Use",
      "body": "Start from the default log root when you need to inspect recent factory runs. Runtime logs are structured JSON rolling files grouped by UTC start date under that root (default ~/.you-agent-factory/logs). Use the retention and rotation controls below when you need a different root, retention window, backup count, size limit, or compression for rotated files. Pass a runtime-log flag on you run to set or inspect the log root for one invocation. CLI --verbose emits concise command diagnostics to stderr; --debug emits lower-level diagnostics where supported and implies --verbose; --json writes structured success output to stdout while diagnostics remain on stderr. Keep runtime logs separate from metrics files (a separate structured JSONL channel) and from record/replay artifacts when diagnosing a run."
    },
    "limitsAndAssumptions": {
      "title": "Limits And Assumptions",
      "body": "Logs covers runtime logs and CLI diagnostics for you-agent-factory. It is not a sync of packaged CLI docs, not the metrics reference, not record/replay artifact management, not FactoryEvent or session event-stream contracts, not agent-run tool-diagnostics inspection, and not the OpenAPI or API reference. Sibling links and packaged you docs topics aid discovery only; they are not required to understand runtime logs and CLI diagnostics here."
    },
    "tags": {
      "title": "Tags"
    }
  },
  "links": {
    "defaultPathLabel": "Default log root",
    "defaultPathValue": "~/.you-agent-factory/logs",
    "retentionFlagHeader": "Flag",
    "retentionEffectHeader": "Effect",
    "runtimeLogDirFlag": "--runtime-log-dir",
    "runtimeLogDirEffect": "Root directory for structured runtime log files grouped by UTC start date (default ~/.you-agent-factory/logs)",
    "runtimeLogMaxAgeFlag": "--runtime-log-max-age-days",
    "runtimeLogMaxAgeEffect": "Maximum days to retain rotated runtime log files (default 30)",
    "runtimeLogMaxBackupsFlag": "--runtime-log-max-backups",
    "runtimeLogMaxBackupsEffect": "Maximum rotated runtime log files to retain (default 20)",
    "runtimeLogMaxSizeFlag": "--runtime-log-max-size-mb",
    "runtimeLogMaxSizeEffect": "Rotate each runtime log file after this many megabytes (default 100)",
    "runtimeLogCompressFlag": "--runtime-log-compress",
    "runtimeLogCompressEffect": "Compress rotated runtime log files",
    "exampleLabel": "Set the runtime log root for one run",
    "exampleCommand": "you run --dir ./factory --runtime-log-dir ~/.you-agent-factory/logs",
    "diagnosticsFlagHeader": "Flag",
    "diagnosticsEffectHeader": "Effect",
    "verboseFlag": "--verbose",
    "verboseEffect": "Emit concise command diagnostics to stderr",
    "debugFlag": "--debug",
    "debugEffect": "Emit lower-level command diagnostics where supported (implies --verbose)",
    "jsonFlag": "--json",
    "jsonEffect": "Emit structured JSON on stdout for supported commands; diagnostics remain on stderr",
    "diagnosticsExampleLabel": "Emit concise stderr diagnostics for one run",
    "diagnosticsExampleCommand": "you run --dir ./factory --verbose",
    "channelBoundaryLabel": "Channel boundaries",
    "systemLogsBoundary": "System logs include command stdout and stderr only on command failures.",
    "environmentDetailsBoundary": "Environment details are record-channel diagnostics only, not the runtime-log channel."
  }
}
