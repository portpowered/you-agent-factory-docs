{
  "title": "Metrics",
  "description": "Watch live you-agent-factory factory-ops metrics for a Factory Session: engine activity, work-token lifecycle, optional resource usage, and the local operator dashboard.",
  "openingSummary": "Factory metrics are the live-run signals operators use to watch a you-agent-factory Factory Session while work is in flight—not Model Atlas evaluation scores or a benchmark leaderboard.",
  "sections": {
    "statusRead": {
      "title": "Read Session Status",
      "body": "Factory metrics are scoped to one live Factory Session. Call GET /factory-sessions/{session_id}/status for a deeper runtime health read than session list or factory query. Replace {session_id} with a live session id from session list—often ~default on single-session hosts. The response carries the factory-ops metrics fields operators watch during a live run: engine activity and runtime status, work-token lifecycle buckets, and optional resource usage when pools are declared."
    },
    "operatorDashboard": {
      "title": "Open The Operator Dashboard",
      "body": "When the service was started via you or you run without quiet mode, open the local operator dashboard as the browser surface for the same live metrics. The dashboard shows live session selection, work position, and factory activity alongside CLI and API status reads."
    },
    "metricsChart": {
      "title": "Watch Metrics Over Time",
      "body": "A one-shot status read is a snapshot. Time-ordered watching means reading the same factory measures across successive ticks so trends become visible. The teaching chart below plots work-token lifecycle buckets — processing, terminal, and failed — over successive status ticks, the same categories totals operators watch while a Factory Session runs."
    },
    "limitsAndAssumptions": {
      "title": "Limits And Assumptions",
      "body": "Metrics covers factory metrics exposure for you-agent-factory operators watching a live Factory Session. It is not a sync of packaged CLI docs, not a logs or diagnostic-event reference, not OpenAPI or API reference ownership, not SPC control-chart depth, not bottleneck diagnosis, and not Model Atlas or benchmark metrics."
    },
    "tags": {
      "title": "Tags"
    }
  },
  "links": {
    "statusReadLabel": "Example status read against the default local host:",
    "statusFieldsIntro": "Read these fields in the status response:",
    "statusFieldHeader": "Field",
    "statusMeaningHeader": "Meaning",
    "statusFactoryStateField": "factoryState",
    "statusFactoryStateMeaning": "Factory lifecycle phase — for example IDLE, RUNNING, PAUSED, COMPLETED, or FAILED.",
    "statusRuntimeStatusField": "runtimeStatus",
    "statusRuntimeStatusMeaning": "Whether the engine is actively processing — IDLE, ACTIVE, or FINISHED.",
    "statusCategoriesField": "categories",
    "statusCategoriesMeaning": "Token counts by lifecycle bucket: initial, processing, terminal, and failed.",
    "statusTotalTokensField": "totalTokens",
    "statusTotalTokensMeaning": "Total tokens across categories.",
    "statusResourcesField": "resources",
    "statusResourcesMeaning": "Optional resource usage entries when the factory declares resources.",
    "statusReadTogetherNote": "A factory can be RUNNING while runtimeStatus is IDLE when the host is up but no work is in flight. Read factoryState, runtimeStatus, and categories together when deciding whether to submit more work or wait for completion.",
    "dashboardUrlLabel": "Default local dashboard URL (service started without quiet mode):",
    "dashboardHostNote": "Use the same host and port as the API unless the process that bound the listener used a different server or port setting."
  }
}
