{
  "title": "Troubleshooting",
  "description": "Recover from you-agent-factory install, run, configuration, MCP, and dynamic-workflow failures with short symptom-scoped guidance that links to the owning canonical docs.",
  "openingSummary": "Troubleshooting is the you-agent-factory recovery lookup for recognizable failure symptoms. Each entry gives a short action you can try, then points at the canonical doc that owns the full contract.",
  "sections": {
    "whatItCovers": {
      "title": "What It Covers",
      "body": "Troubleshooting is the you-agent-factory recovery lookup for install, first-run, configuration, MCP, and dynamic-workflow failures. Use it when something fails and you need a scannable symptom, a short recovery action, and a link into the owning canonical doc rather than a second copy of that doc."
    },
    "keyConcepts": {
      "title": "Key Concepts",
      "body": "A failure entry pairs a recognizable symptom with a short recovery action and a pointer to the canonical documentation page that owns the deeper contract. Recovery stays symptom-scoped: try the short action first, then open install, getting started, CLI, configuration, MCP, dynamic workflows, or Factory Session when you need the full reference. This surface does not replace those pages."
    },
    "howToUse": {
      "title": "How To Use",
      "body": "Scan for the symptom that matches what you see, try the short recovery action, then follow the link into the owning canonical doc when you need commands, flags, or deeper setup. Use this page for recovery lookup. Open the linked install, getting-started, CLI, configuration, MCP, dynamic-workflows, or Factory Session pages when you need the full contract those surfaces own."
    },
    "commandNotFound": {
      "title": "you Not Found After Install",
      "body": "Symptom: your shell reports you: command not found, or which you finds nothing, after you ran an install script. Recovery: open a new terminal so PATH reloads, then confirm you resolves. If the binary is still missing, re-run the install script for your operating system and confirm the script finished without error before retrying you. For the full OS install paths and post-install scaffold notes, open Install."
    },
    "osInstallOrInit": {
      "title": "OS Install Or Executor Init Confusion",
      "body": "Symptom: the install path does not match your OS, or you are unsure which you init starter scaffold to use after install. Recovery: use the macOS/Linux curl script or the Windows PowerShell script for your machine, then scaffold with you init for the default Codex-backed starter, or you init --executor claude when you want a Claude-backed starter. Do not mix OS install entrypoints. For the install reference and first-run order, open Install and Getting Started."
    },
    "noLiveFactory": {
      "title": "Submit Or Work Against A Factory That Is Not Live",
      "body": "Symptom: you session list is empty, or the host connection fails, when you try to submit or inspect work. Recovery: start or attach a factory with you or you run first, then confirm a live Factory Session with you session list before you submit. An empty list means the host responded but no session is registered yet; a connection failure means nothing is listening—start the factory and retry. For session liveness and the broader command matrix, open Factory Session, Getting Started, and CLI."
    },
    "operatorDefaultsStartup": {
      "title": "Operator Defaults Fail At Startup",
      "body": "Symptom: the factory fails before dispatch with a config-path error, an unsupported workerModelProvider summary, or DEFAULT resolution guidance—or you expected operator defaults and nothing applied. Recovery: a missing ~/.you-agent-factory/config.json is valid and applies no defaults; create the file only when you want defaults.workerModelProvider and defaults.workerModel. Fix malformed JSON so the file parses, replace unsupported providers with an accepted value, and never leave DEFAULT without a concrete lower-precedence provider from file, env, or flag. For operator-default failure modes and named factories, open Global Configuration Factories; for factory.json topology, open Configuration."
    },
    "mcpPath": {
      "title": "MCP Host Cannot Resolve you On PATH",
      "body": "Symptom: the MCP host fails to spawn you mcp serve because command you is not found on the host PATH. Recovery: set the host command field to the absolute path of the you binary instead of relying on the shell PATH, keep args as mcp serve, then reload the host so the child process respawns. For host JSON shape and serve modes, open MCP; for a Cursor first-loop walkthrough, open the Cursor dynamic workflows guide."
    },
    "mcpWrongCwd": {
      "title": "MCP Sources Fail Because cwd Is Wrong",
      "body": "Symptom: validate or start fails to resolve workflow sources even though the MCP child process starts. Recovery: set cwd to the absolute path of the workflow or project root whose sources Factory Session tools should resolve—not a nested package, home directory, or unrelated checkout—then retry validate. For host cwd notes, open MCP; for dynamic-workflow source and configuration depth, open Dynamic Workflows."
    },
    "mcpHostNotReloaded": {
      "title": "MCP Host Not Reloaded After Config Save",
      "body": "Symptom: you saved MCP host configuration, but Factory Session tools still do not appear under you.factory_session.* names. Recovery: restart the host or run its reload-window action so it respawns the MCP child process—saving the file alone is not enough for most hosts. After reload, confirm the tools appear before calling validate or start. For reload and tool discovery, open MCP; for Cursor reload steps, open the Cursor dynamic workflows guide."
    },
    "cursorDynamicWorkflow": {
      "title": "Cursor Dynamic Workflow Loop Fails",
      "body": "Symptom: Cursor cannot complete a first Factory Session loop—tools missing after MCP setup, validate_source fails, or start/status never reaches a live session. Recovery: confirm you mcp serve is configured with an absolute command path when needed, cwd at the workflow project root, and a host reload after every MCP config change, then retry validate → start → status or result. Treat the Cursor guide as the first-loop walkthrough; open Dynamic Workflows and MCP when you need API or host-contract depth beyond that loop."
    },
    "limitsAndAssumptions": {
      "title": "Limits And Assumptions",
      "body": "This page is a web recovery lookup for you-agent-factory failure modes. It is not the install command matrix, not the getting-started quickstart, not a full CLI flag dump, not the configuration or global-configuration-factories deep dive, not MCP host setup, not dynamic-workflow authoring, and not a sync of packaged you docs. Sibling links aid discovery only; they are not required to understand the short recovery actions here."
    },
    "related": {
      "title": "Related To"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "links": {
    "installDocs": "Install",
    "gettingStarted": "Getting Started",
    "cliDocs": "CLI",
    "factorySession": "Factory Session",
    "configurationDocs": "Configuration",
    "globalConfigurationFactories": "Global Configuration Factories",
    "mcpDocs": "MCP",
    "dynamicWorkflows": "Dynamic Workflows",
    "cursorDynamicWorkflows": "Cursor dynamic workflows guide",
    "faq": "FAQ"
  }
}
