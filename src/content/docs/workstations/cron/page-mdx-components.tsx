/**
 * Page-local MDX components for workstations/cron.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { CronBehaviorExamples } from "./CronBehaviorExamples";
import { CronBehaviorVariantSchemaEmbed } from "./CronBehaviorVariantSchemaEmbed";

export const pageMdxComponents = {
  CronBehaviorExamples,
  CronBehaviorVariantSchemaEmbed,
};
