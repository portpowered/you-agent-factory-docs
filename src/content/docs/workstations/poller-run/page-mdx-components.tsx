/**
 * Page-local MDX components for workstations/poller-run.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { PollerRunTypeExamples } from "./PollerRunTypeExamples";
import { PollerRunTypeVariantSchemaEmbed } from "./PollerRunTypeVariantSchemaEmbed";

export const pageMdxComponents = {
  PollerRunTypeExamples,
  PollerRunTypeVariantSchemaEmbed,
};
