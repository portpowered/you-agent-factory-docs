/**
 * Page-local MDX components for workstations/inference-run.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { InferenceRunTypeExamples } from "./InferenceRunTypeExamples";
import { InferenceRunTypeVariantSchemaEmbed } from "./InferenceRunTypeVariantSchemaEmbed";

export const pageMdxComponents = {
  InferenceRunTypeExamples,
  InferenceRunTypeVariantSchemaEmbed,
};
