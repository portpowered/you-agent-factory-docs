/**
 * Page-local MDX components for workstations/standard.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { StandardBehaviorExamples } from "./StandardBehaviorExamples";
import { StandardBehaviorVariantSchemaEmbed } from "./StandardBehaviorVariantSchemaEmbed";

export const pageMdxComponents = {
  StandardBehaviorExamples,
  StandardBehaviorVariantSchemaEmbed,
};
