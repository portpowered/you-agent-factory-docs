/**
 * Page-local MDX components for workstations/logical-move.
 * Merged by route-family-local-docs-page-load when this slug is requested.
 * Do not register these in the shared module MDX map.
 */
import { LogicalMoveTypeExamples } from "./LogicalMoveTypeExamples";
import { LogicalMoveTypeVariantSchemaEmbed } from "./LogicalMoveTypeVariantSchemaEmbed";

export const pageMdxComponents = {
  LogicalMoveTypeExamples,
  LogicalMoveTypeVariantSchemaEmbed,
};
