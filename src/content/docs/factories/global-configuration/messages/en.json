{
  "title": "Global Configuration",
  "description": "Set you-agent-factory operator model defaults and manage global / named factories under ~/.you-agent-factory.",
  "openingSummary": "Global configuration covers operator-wide model defaults in ~/.you-agent-factory/config.json and shared named factories under ~/.you-agent-factory/factories — separate from project factory.json topology.",
  "sections": {
    "operatorModelDefaults": {
      "title": "Operator Model Defaults",
      "body": "Use operator defaults when you want one preferred provider and model across runs without editing every factory worker. Defaults apply only to INFERENCE_WORKER and AGENT_WORKER entries that omit modelProvider or model. Authored worker values stay as written. Script workers and poller workers never receive operator defaults. The runtime fills omitted fields in memory at startup and does not persist those fills into factory.json."
    },
    "youConfigSchema": {
      "title": "You Configuration Schema",
      "body": "The live you-config schema is the contract for ~/.you-agent-factory/config.json. defaults holds operator model preferences parsed from file before env and flag resolution. workerPresets names reusable file-only worker model presets. backendScopeID identifies the local provider-backed runtime boundary. Open the full schema and API references when you need exhaustive contract lookup beyond this teaching embed."
    },
    "namedFactories": {
      "title": "Named Factories",
      "body": "Named factories are persisted factory definitions under a factory root. The project-local root defaults to ./factory (or the path you pass with --dir). The global shared root is ~/.you-agent-factory/factories. Use you run --named <name> to resolve a canonical persisted name: project-local first, then the global shared root, then built-in catalog materialization on first use. Listing, saving, updating, and deleting always target exactly one root via --dir; they never merge project-local and global entries in one command."
    },
    "factoryNameSchema": {
      "title": "Factory Name Contract",
      "body": "The live FactoryName definition is the customer-facing identifier for one stored named factory. Named resolution and factory-session lookup use that identity. Exhaustive Factory field inventories stay on the full schema and API reference pages."
    },
    "factoryRunnerSchema": {
      "title": "Factory Runner Versus Operator Defaults",
      "body": "Factory-level runner in factory.json is topology-owned and distinct from operator model defaults. The live RunnerID contract lists built-in runner identifiers for factory and workstation runner selection. Operator defaults do not rewrite runner fields. Set operator defaults or author modelProvider on workers when you need a preferred model worker provider — do not expect runner to stand in for operator defaults."
    },
    "howToUse": {
      "title": "How To Use",
      "body": "Create ~/.you-agent-factory/config.json with defaults.workerModelProvider and defaults.workerModel when you want one preferred provider and model across runs. Override a single invocation with YOU_DEFAULT_WORKER_MODEL_PROVIDER and YOU_DEFAULT_WORKER_MODEL, or with --default-worker-model-provider and --default-worker-model on the root you command. Precedence is independent per field: file, then env, then flag. For named factories, list one root at a time, run with you run --named, and save or update under the --dir root you intend to own. Use the factories configuration page when you need factory.json topology instead of operator or global factory settings. Packaged @you/* invocation signatures stay on their sibling docs surfaces."
    },
    "limitsAndAssumptions": {
      "title": "Limits And Assumptions",
      "body": "This page is the web operator-defaults and global / named-factories reference. It is not a sync of packaged CLI docs, not the factory.json topology overview, not workers, workstations, or resources field dumps, not packaged @you/* invocation deep dives, not factory-session, and not submitting-work. Use the related factories and documentation links for factory.json topology, CLI command surfaces, and worker field contracts. Operator-default startup failures stay short here: missing config continues with no defaults; malformed config, unsupported providers, and DEFAULT without a concrete lower-precedence provider fail before dispatch. Exhaustive System config and Factory field inventories live on the schema and API reference pages linked from the schema sections."
    },
    "related": {
      "title": "Related To"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "links": {
    "configPathLabel": "Default config path:",
    "configExampleLabel": "Example operator config:",
    "envOverridesLabel": "Environment overrides (each field independently):",
    "envVariableHeader": "Variable",
    "envPurposeHeader": "Purpose",
    "envProviderName": "YOU_DEFAULT_WORKER_MODEL_PROVIDER",
    "envProviderPurpose": "Overrides defaults.workerModelProvider from the config file",
    "envModelName": "YOU_DEFAULT_WORKER_MODEL",
    "envModelPurpose": "Overrides defaults.workerModel from the config file",
    "flagOverridesLabel": "Global CLI flags (highest precedence per field):",
    "flagNameHeader": "Flag",
    "flagPurposeHeader": "Purpose",
    "flagProviderName": "--default-worker-model-provider",
    "flagProviderPurpose": "Overrides file and environment for the worker model provider",
    "flagModelName": "--default-worker-model",
    "flagModelPurpose": "Overrides file and environment for the worker model",
    "globalRootLabel": "Global shared factories root:",
    "projectLocalRootLabel": "Default project-local root:",
    "listProjectLocalLabel": "List named factories under the project-local root:",
    "listGlobalLabel": "List named factories under the global shared root:",
    "runNamedLabel": "Run a named factory (project-local first, then global, then built-in materialization):",
    "saveNamedLabel": "Save a named factory under a chosen root:",
    "updateNamedLabel": "Update an existing named factory under a chosen root:",
    "failureModesLabel": "Operator-default failure modes:",
    "failureConditionHeader": "Condition",
    "failureBehaviorHeader": "Behavior",
    "failureMissingConfigCondition": "Missing ~/.you-agent-factory/config.json",
    "failureMissingConfigBehavior": "Startup continues with no operator defaults",
    "failureMalformedConfigCondition": "Malformed config JSON",
    "failureMalformedConfigBehavior": "Fails before service construction; error names the config path",
    "failureUnsupportedProviderCondition": "Unsupported workerModelProvider",
    "failureUnsupportedProviderBehavior": "Fails before dispatch with accepted provider summary",
    "failureDefaultWithoutProviderCondition": "DEFAULT without a lower-precedence concrete provider",
    "failureDefaultWithoutProviderBehavior": "Fails before dispatch with resolution guidance",
    "failureAuthoredWorkerFieldsCondition": "Authored worker modelProvider or model",
    "failureAuthoredWorkerFieldsBehavior": "Operator defaults do not override authored values",
    "fullSystemConfigSchema": "Full System config schema",
    "fullFactorySchema": "Full Factory schema",
    "fullFactoryApi": "Factory API reference",
    "configurationDocs": "Configuration (factory.json topology)",
    "packagedDocs": "Packaged factories",
    "dynamicWorkflowsDocs": "Dynamic workflows",
    "sessionsDocs": "Factory sessions",
    "cliDocs": "CLI",
    "workersDocs": "Workers",
    "workstationsDocs": "Workstations",
    "resourcesDocs": "Resources"
  },
  "callouts": {
    "configFileNotes": {
      "body": "A missing config file is valid and applies no operator defaults. Malformed JSON fails before service construction with an error that names the config file path. Unknown top-level JSON fields are rejected. Empty or unset environment values do not clear a lower-precedence layer."
    },
    "flagExample": {
      "body": "These are global persistent flags on the root you command. Both bare you and explicit you run honor them:"
    },
    "precedence": {
      "body": "Effective precedence is independent per field. For each of workerModelProvider and workerModel, the winning layer is:"
    },
    "schemaEmbedNote": {
      "body": "Teaching embeds show the live root and addressed definitions that matter on this page. They are not a substitute for the full schema or API inventories."
    },
    "namedResolution": {
      "body": "you run --named <name> resolves a canonical persisted factory name in this order:"
    },
    "oneRootListing": {
      "body": "you factory list lists exactly one root at a time and never merges project-local and global entries. By default it lists ./factory. Point --dir at ~/.you-agent-factory/factories to list global built-ins and shared factories."
    },
    "builtInMaterialization": {
      "body": "Built-ins such as @you/goal, @you/fusion, and @you/tts materialize into the global root on first use and remain editable on disk for later runs. Deep packaged invocation signatures stay on sibling docs surfaces; this page only covers where those factories live and how named resolution finds them."
    }
  }
}
