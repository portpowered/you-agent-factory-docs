{
  "title": "RMSNorm",
  "description": "Per-token root-mean-square scaling that rescales each hidden vector without mean centering, as used in many modern decoder transformers.",
  "openingSummary": "RMSNorm keeps each token's hidden vector in a stable scale band by dividing by its root mean square instead of using full mean-and-variance normalization, which makes it a lighter fit for many modern decoder-only transformer stacks.",
  "sections": {
    "whatItIs": {
      "title": "What It Is",
      "body": "Root mean square normalization rescales one token's hidden vector by dividing by the root mean square of its features, then applying learned gain. Unlike layer norm, it does not subtract the feature mean first."
    },
    "whyItExists": {
      "title": "Why It Exists",
      "body": "RMSNorm targets stable hidden-state scale with less normalization work than full layer norm. It keeps per-token magnitudes predictable while avoiding the mean-centering step."
    },
    "howItWorks": {
      "title": "How It Works",
      "body": "For one token vector, RMSNorm computes the root mean square over the features, divides the vector by that stabilized scale, then applies learned gain. The switcher below keeps that path next to layer norm so you can see that RMSNorm preserves the same per-token pattern while skipping mean subtraction."
    },
    "mathOrComputeSchema": {
      "title": "Math Or Compute Schema",
      "body": "These two formulas both normalize one token vector at a time. RMSNorm keeps the scale correction from layer norm but removes the mean-centering term."
    },
    "comparedToNearbyModules": {
      "title": "Compared To Nearby Modules",
      "body": "RMSNorm is closest to layer norm because both normalize one token at a time. The main difference is that RMSNorm keeps the scale correction but drops mean centering and often omits the learned bias term as well."
    },
    "exampleArchitectures": {
      "title": "Example Architectures",
      "body": "RMSNorm appears in many recent decoder transformers, especially Llama-style stacks and related architectures that prefer a lighter normalization rule than classic layer norm."
    },
    "limitationsAndTradeoffs": {
      "title": "Limitations And Tradeoffs",
      "body": "RMSNorm gives up explicit mean centering, so it is not identical to layer norm. In setups that benefit from subtracting the feature mean, designers may still prefer the fuller layer norm rule."
    },
    "whyItStillMatters": {
      "title": "Why It Still Matters",
      "body": "RMSNorm is now one of the most common normalization names in modern LLM writeups. Knowing how it differs from layer norm helps you read those architectures without treating the label as a black box."
    },
    "related": {
      "title": "Related"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "assets": {
    "computeFlow": {
      "alt": "Normalization comparison showing LayerNorm and RMSNorm as two per-token normalization paths in the same transformer slot",
      "variants": {
        "layer": {
          "label": "LayerNorm"
        },
        "rms": {
          "label": "RMSNorm"
        }
      }
    },
    "comparisonTable": {}
  },
  "tables": {
    "comparison": {
      "columns": {
        "rms": {
          "title": "RMSNorm"
        },
        "layer": {
          "title": "Layer Norm"
        },
        "batch": {
          "title": "Batch Norm"
        }
      },
      "dimensions": {
        "meanCentering": "Mean centering",
        "learnedShift": "Learned shift term",
        "commonPlacement": "Common placement"
      },
      "values": {
        "rms": {
          "meanCentering": "No; scales by root mean square only",
          "learnedShift": "Often omitted; learned gain is the main parameter",
          "commonPlacement": "Per-token norm inside modern decoder transformers"
        },
        "layer": {
          "meanCentering": "Yes; subtracts the token's feature mean",
          "learnedShift": "Usually includes both learned gain and bias",
          "commonPlacement": "Per-token norm around transformer sublayers"
        },
        "batch": {
          "meanCentering": "Yes; uses minibatch feature statistics",
          "learnedShift": "Usually includes both learned gain and bias",
          "commonPlacement": "Feature or channel norm across image-style minibatches"
        }
      }
    }
  },
  "math": {
    "rmsNormSchema": {
      "label": "RMS normalization",
      "formula": "\\mathrm{RMSNorm}(x) = \\gamma \\odot \\frac{x}{\\sqrt{\\frac{1}{d}\\sum_{i=1}^{d} x_i^2 + \\epsilon}}",
      "variableDefinitions": {
        "x": {
          "term": "x",
          "definition": "Hidden feature vector at one token position."
        },
        "d": {
          "term": "d",
          "definition": "Number of features in x."
        },
        "epsilon": {
          "term": "\\epsilon",
          "definition": "Small constant for numerical stability."
        },
        "gamma": {
          "term": "\\gamma",
          "definition": "Learned per-feature scale."
        }
      }
    },
    "layerNormSchema": {
      "label": "Layer normalization",
      "formula": "\\mathrm{LN}(x) = \\gamma \\odot \\frac{x - \\mu}{\\sqrt{\\sigma^2 + \\epsilon}} + \\beta",
      "variableDefinitions": {
        "x": {
          "term": "x",
          "definition": "Hidden feature vector at one token position."
        },
        "mu": {
          "term": "\\mu",
          "definition": "Mean of the features in x."
        },
        "sigma2": {
          "term": "\\sigma^2",
          "definition": "Variance of the features in x."
        },
        "epsilon": {
          "term": "\\epsilon",
          "definition": "Small constant for numerical stability."
        },
        "gamma": {
          "term": "\\gamma",
          "definition": "Learned per-feature scale."
        },
        "beta": {
          "term": "\\beta",
          "definition": "Learned per-feature shift."
        }
      }
    }
  },
  "graph": {
    "nodes": {
      "tokenState": {
        "label": "Token state x"
      },
      "rmsEstimate": {
        "label": "Root mean square over features"
      },
      "scaleVector": {
        "label": "Divide by RMS scale only"
      },
      "learnedGain": {
        "label": "Apply learned gain γ"
      },
      "nextSublayer": {
        "label": "Stable token for next sublayer"
      },
      "perTokenStats": {
        "label": "Statistics still come from one token vector, not from the batch"
      },
      "noCenteringNote": {
        "label": "No mean subtraction step: RMSNorm corrects magnitude without recentering the vector"
      }
    }
  }
}
