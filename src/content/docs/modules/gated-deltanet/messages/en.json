{
  "title": "Gated DeltaNet",
  "description": "A sequence-mixing module that updates a compact recurrent memory state with input-dependent gating and delta-rule writes instead of materializing a full attention matrix.",
  "openingSummary": "Gated Delta Networks, often shortened to Gated DeltaNet or GDN, are a sequence-mixing module that keeps a fixed-size recurrent memory state and updates it with gated decay plus targeted delta-rule writes rather than scoring every token pair in a full attention matrix.",
  "sections": {
    "whatItIs": {
      "title": "What It Is",
      "body": "Gated Delta Networks are a sequence-mixing module used in efficient long-context language models. Gated DeltaNet is the common shorthand for the same design. At each position the module projects the hidden state into query, key, and value signals, reads from a compact matrix-valued memory state, and writes back an updated state for the next step. The past is carried forward through that recurrent state instead of through an explicit all-pairs attention map."
    },
    "whyItExists": {
      "title": "Why It Exists",
      "body": "Dense attention scales poorly as context length grows because every query token can compare against every earlier key. Linear attention lowers that cost by accumulating key-value statistics, but simple accumulation can blur or collide older information inside a fixed-size state. Gated Delta Networks exist to keep long-context sequence mixing affordable while giving the model stronger control over memory: gates can clear or retain past content, and the delta rule can replace a targeted slot instead of only adding another outer-product term."
    },
    "howItWorks": {
      "title": "How It Works",
      "body": "Each step starts from the current hidden state and projects it into query, key, and value vectors. The gate α_t is a separate control path: it scales how much of the previous memory matrix S_{t-1} survives before any new content is written. The delta-rule path is different: it uses the current key and value to perform a targeted edit, replacing the memory content associated with k_t instead of only adding another outer-product term. After that gated write completes, the updated state S_t is stored for the next position and the current query q_t reads the result to form the layer output o_t. The diagram below keeps the gate decay step and the delta-rule write as distinct stages rather than one generic memory box."
    },
    "mathOrComputeSchema": {
      "title": "Math Or Compute Schema",
      "body": "Dense multi-head attention materializes softmax weights over all earlier keys. Gated Delta Networks instead keep a matrix-valued memory state S_t. The gate α_t controls memory decay on the previous state, β_t scales a targeted delta-rule write built from v_t and the residual (v_t - S_{t-1} k_t), and the output o_t = S_t q_t reads the updated memory with the current query. The formulas below contrast the dense attention path with that gated delta recurrence."
    },
    "comparedToNearbyModules": {
      "title": "Compared To Nearby Modules",
      "body": "Multi-head attention keeps the full pairwise score matrix and remains the most direct way to address any earlier token. Linear attention compresses the past into a running summary, but ordinary accumulation-only updates can be harder to erase or replace precisely. DeltaNet-style updates use the same delta-rule write without the separate input-dependent gate that scales memory decay, so older content can linger unless another write overwrites it. Mamba2-style gating controls state decay in selective state-space mixers, but it does not use the same targeted key-value delta edit that Gated Delta Networks apply on top of their memory matrix. Sliding-window attention and sparse attention stay closer to dense softmax attention, yet they only connect tokens inside a local band or chosen pattern. Gated DeltaNet keeps one compact global recurrent state and updates it selectively at each step. That design trades direct token-by-token addressability for near-linear long-context cost: gated decay plus delta-rule editing can clear or replace memory slots without materializing an n-by-n attention map. Hybrid stacks that mix Gated DeltaNet with sliding-window attention or sparse attention are common when a model needs both efficient global mixing and direct local or patterned access."
    },
    "exampleArchitectures": {
      "title": "Example Architectures",
      "body": "The source paper studies decoder language models that replace some attention layers with Gated DeltaNet blocks and reports hybrid stacks that mix Gated DeltaNet with sliding-window attention or Mamba2-style layers. Qwen3.5-0.8B is one shipped model family on this site that routes part of its stack through Gated DeltaNet layers."
    },
    "limitationsAndTradeoffs": {
      "title": "Limitations And Tradeoffs",
      "body": "The main tradeoff is retrieval fidelity. A fixed-size recurrent state cannot store every past token independently the way dense attention can, because the past is compressed rather than directly addressable token by token. Retrieval quality depends on how cleanly keys, gates, and delta writes use the available state width, so very long contexts can still stress the compressed memory even when compute stays affordable. Hybrid designs that mix Gated DeltaNet with sliding-window attention, sparse attention, or dense attention remain common when a model needs both efficient global mixing and direct token access."
    },
    "whyItStillMatters": {
      "title": "Why It Still Matters",
      "body": "Gated Delta Networks sit on a practical path between quadratic attention and simpler linear recurrent mixers. They show how gated memory control and delta-rule editing can be combined in one hardware-friendly recurrence, which makes them a useful reference when reading newer efficient long-context model stacks."
    },
    "related": {
      "title": "Related"
    },
    "tags": {
      "title": "Tags"
    },
    "references": {
      "title": "References"
    }
  },
  "assets": {
    "computeFlow": {
      "alt": "Dense multi-head attention versus Gated DeltaNet with gated decay, delta-rule memory update, and recurrent state readout",
      "caption": "Gated DeltaNet keeps one compact memory matrix. Gate α_t controls decay on S_{t-1}, the delta-rule path performs the targeted write into S_t, and the query reads o_t = S_t q_t instead of building a full attention matrix.",
      "title": "Gated DeltaNet compute path",
      "legend": {
        "data-flow": {
          "label": "Projections, memory, and readout"
        },
        "control-flow": {
          "label": "Gate decay control"
        },
        "summary-node": {
          "label": "Gating or memory stage"
        },
        "process-node": {
          "label": "Input or output step"
        },
        "annotation": {
          "label": "Mechanism note"
        }
      },
      "variants": {
        "mha": {
          "label": "Multi-head"
        },
        "gdn": {
          "label": "Gated DeltaNet"
        }
      }
    },
    "comparisonTable": {}
  },
  "tables": {
    "comparison": {
      "columns": {
        "gdn": {
          "title": "Gated DeltaNet"
        },
        "linear": {
          "title": "Linear Attention"
        },
        "mha": {
          "title": "Multi-Head Attention"
        },
        "window": {
          "title": "Sliding-Window Attention"
        }
      },
      "dimensions": {
        "memoryForm": "Memory form",
        "sequenceScaling": "Sequence scaling",
        "updateStyle": "Update style",
        "pastAddressability": "Past addressability"
      },
      "values": {
        "gdn": {
          "memoryForm": "Fixed-size matrix-valued recurrent state carried across positions",
          "sequenceScaling": "Near-linear O(n) per layer with constant state width",
          "updateStyle": "Gated decay plus targeted delta-rule memory edits",
          "pastAddressability": "Indirect; the past is compressed into one recurrent state rather than stored per token"
        },
        "linear": {
          "memoryForm": "Running key-value summary accumulated across positions",
          "sequenceScaling": "Near-linear O(n) per head with fixed feature-map dimension",
          "updateStyle": "Associative accumulation of feature-mapped keys and values",
          "pastAddressability": "Indirect; earlier tokens are summarized in accumulated statistics"
        },
        "mha": {
          "memoryForm": "Explicit attention weights over all earlier tokens",
          "sequenceScaling": "Quadratic O(n²) per head",
          "updateStyle": "Softmax-weighted sum over all allowed key positions",
          "pastAddressability": "Direct; any earlier token can be scored explicitly"
        },
        "window": {
          "memoryForm": "Dense attention inside a fixed local window",
          "sequenceScaling": "Linear O(n·W) per head when window width W is fixed",
          "updateStyle": "Softmax over keys inside the local band only",
          "pastAddressability": "Direct only inside the local window; distant tokens need other layers or patterns"
        }
      }
    }
  },
  "math": {
    "mhaSchema": {
      "label": "Multi-head attention (MHA)",
      "formula": "\\text{Attention}(Q_i, K_i, V_i) = \\mathrm{softmax}\\!\\left(\\frac{Q_i K_i^{\\top}}{\\sqrt{d_k}}\\right) V_i",
      "variableDefinitions": {
        "q": {
          "term": "Q",
          "definition": "Query vectors for head i."
        },
        "k": {
          "term": "K",
          "definition": "Key vectors for head i."
        },
        "v": {
          "term": "V",
          "definition": "Value vectors for head i."
        },
        "h": {
          "term": "H",
          "definition": "Number of query heads."
        },
        "dk": {
          "term": "d_k",
          "definition": "Key dimension per head."
        },
        "i": {
          "term": "i",
          "definition": "Query head index."
        }
      }
    },
    "gdnSchema": {
      "label": "Gated DeltaNet",
      "formula": "S_t = \\alpha_t S_{t-1} + \\beta_t\\,(v_t - S_{t-1} k_t)\\,k_t^{\\top},\\quad o_t = S_t q_t",
      "variableDefinitions": {
        "qt": {
          "term": "q_t",
          "definition": "Current-step query vector."
        },
        "kt": {
          "term": "k_t",
          "definition": "Current-step key vector."
        },
        "vt": {
          "term": "v_t",
          "definition": "Current-step value vector."
        },
        "g": {
          "term": "\\alpha_t",
          "definition": "Input-dependent gate that decays S_{t-1}."
        },
        "gi": {
          "term": "\\beta_t",
          "definition": "Input-dependent write strength for the delta-rule update."
        },
        "s": {
          "term": "S_t",
          "definition": "Matrix-valued memory state after gated decay and the delta write."
        },
        "o": {
          "term": "o_t",
          "definition": "Layer output read from S_t with q_t."
        }
      }
    }
  },
  "graph": {
    "nodes": {
      "tokenInput": {
        "label": "Token hidden state"
      },
      "qkvProjections": {
        "label": "Q, K, V projections"
      },
      "gateSignal": {
        "label": "Gate α_t"
      },
      "memoryStatePrev": {
        "label": "Memory S_{t-1}"
      },
      "deltaUpdate": {
        "label": "Delta-rule write"
      },
      "memoryStateNext": {
        "label": "Updated memory S_t"
      },
      "outputReadout": {
        "label": "Output o_t = S_t q_t"
      },
      "gatingDeltaNote": {
        "label": "Gating controls decay; the delta rule performs the targeted write"
      }
    }
  }
}
