/**
 * AI domain model/module/paper/training/system renderers were retired with
 * `src/features/models`. This module remains as an empty namespace surface so
 * existing `@/features/ai/models` import paths do not break during the rewrite.
 */

export {};
