/** Stable scaffold identifier used by health checks and smoke tests. */
export const SCAFFOLD_ID = "model-reference-scaffold" as const;

/** Brand label rendered on the home page and shared shell surfaces. */
export const SITE_BRAND_NAME = "Model Atlas" as const;

/** Primary home page heading. */
export const SITE_HEADING = "Model Reference" as const;
